// Code generated by Fern. DO NOT EDIT.

package subscription_test

import (
	bytes "bytes"
	context "context"
	json "encoding/json"
	payabli "github.com/payabli/sdk-go"
	client "github.com/payabli/sdk-go/client"
	option "github.com/payabli/sdk-go/option"
	require "github.com/stretchr/testify/require"
	http "net/http"
	testing "testing"
)

func ResetWireMockRequests(
	t *testing.T,
) {
	WiremockAdminURL := "http://localhost:8080/__admin"
	_, err := http.Post(WiremockAdminURL+"/requests/reset", "application/json", nil)
	require.NoError(t, err)
}

func VerifyRequestCount(
	t *testing.T,
	method string,
	urlPath string,
	queryParams map[string]string,
	expected int,
) {
	WiremockAdminURL := "http://localhost:8080/__admin"
	var reqBody bytes.Buffer
	reqBody.WriteString(`{"method":"`)
	reqBody.WriteString(method)
	reqBody.WriteString(`","urlPath":"`)
	reqBody.WriteString(urlPath)
	reqBody.WriteString(`"}`)
	if len(queryParams) > 0 {
		reqBody.WriteString(`,"queryParameters":{`)
		first := true
		for key, value := range queryParams {
			if !first {
				reqBody.WriteString(",")
			}
			reqBody.WriteString(`"`)
			reqBody.WriteString(key)
			reqBody.WriteString(`":{"equalTo":"`)
			reqBody.WriteString(value)
			reqBody.WriteString(`"}`)
			first = false
		}
		reqBody.WriteString("}")
	}
	resp, err := http.Post(WiremockAdminURL+"/requests/find", "application/json", &reqBody)
	require.NoError(t, err)
	var result struct {
		Requests []interface{} `json:"requests"`
	}
	json.NewDecoder(resp.Body).Decode(&result)
	require.Equal(t, expected, len(result.Requests))
}

func TestSubscriptionGetSubscriptionWithWireMock(
	t *testing.T,
) {
	ResetWireMockRequests(t)
	WireMockBaseURL := "http://localhost:8080"
	client := client.NewClient(
		option.WithBaseURL(
			WireMockBaseURL,
		),
	)
	_, invocationErr := client.Subscription.GetSubscription(
		context.TODO(),
		263,
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "GET", "/Subscription/263", nil, 1)
}

func TestSubscriptionNewSubscriptionWithWireMock(
	t *testing.T,
) {
	ResetWireMockRequests(t)
	WireMockBaseURL := "http://localhost:8080"
	client := client.NewClient(
		option.WithBaseURL(
			WireMockBaseURL,
		),
	)
	request := &payabli.RequestSchedule{
		Body: &payabli.SubscriptionRequestBody{
			CustomerData: &payabli.PayorDataRequest{
				CustomerId: payabli.Int64(
					4440,
				),
			},
			EntryPoint: payabli.String(
				"f743aed24a",
			),
			PaymentDetails: &payabli.PaymentDetail{
				ServiceFee: payabli.Float64(
					0,
				),
				TotalAmount: 100,
			},
			PaymentMethod: &payabli.RequestSchedulePaymentMethod{
				PayMethodCredit: &payabli.PayMethodCredit{
					Cardcvv: payabli.String(
						"123",
					),
					Cardexp: "02/25",
					CardHolder: payabli.String(
						"John Cassian",
					),
					Cardnumber: "4111111111111111",
					Cardzip: payabli.String(
						"37615",
					),
					Initiator: payabli.String(
						"payor",
					),
				},
			},
			ScheduleDetails: &payabli.ScheduleDetail{
				EndDate: payabli.String(
					"03-20-2025",
				),
				Frequency: payabli.FrequencyWeekly.Ptr(),
				PlanId: payabli.Int(
					1,
				),
				StartDate: payabli.String(
					"09-20-2024",
				),
			},
		},
	}
	_, invocationErr := client.Subscription.NewSubscription(
		context.TODO(),
		request,
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "POST", "/Subscription/add", nil, 1)
}

func TestSubscriptionRemoveSubscriptionWithWireMock(
	t *testing.T,
) {
	ResetWireMockRequests(t)
	WireMockBaseURL := "http://localhost:8080"
	client := client.NewClient(
		option.WithBaseURL(
			WireMockBaseURL,
		),
	)
	_, invocationErr := client.Subscription.RemoveSubscription(
		context.TODO(),
		396,
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "DELETE", "/Subscription/396", nil, 1)
}

func TestSubscriptionUpdateSubscriptionWithWireMock(
	t *testing.T,
) {
	ResetWireMockRequests(t)
	WireMockBaseURL := "http://localhost:8080"
	client := client.NewClient(
		option.WithBaseURL(
			WireMockBaseURL,
		),
	)
	request := &payabli.RequestUpdateSchedule{
		SetPause: payabli.Bool(
			true,
		),
	}
	_, invocationErr := client.Subscription.UpdateSubscription(
		context.TODO(),
		231,
		request,
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "PUT", "/Subscription/231", nil, 1)
}
